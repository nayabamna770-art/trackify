import 'package:flutter/material.dart';

/// Glassmorphic constants, opacity constraints, and blur parameters
class AppGlassStyle {
  static const double borderThickness = 1.5;
  static const double defaultCornerRadius = 20.0;
  static const double blurSigmaX = 15.0;
  static const double blurSigmaY = 15.0;

  // Default interactive opacity level (adjustable via slider 0.05 to 0.40)
  static const double defaultOpacity = 0.15;
}